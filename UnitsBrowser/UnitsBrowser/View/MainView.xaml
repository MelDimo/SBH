<UserControl x:Class="com.sbh.gui.unitsbrowser.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:com.sbh.gui.unitsbrowser.View"
             xmlns:viewmodel="clr-namespace:com.sbh.gui.unitsbrowser.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView ItemsSource="{Binding CollectionUnitEx}" SelectedItem="{Binding CurrentUnitEx}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding UnitName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Height="32" Width="32" Command="{Binding UnitOnClickCommand}"/>
    </Grid>
</UserControl>
